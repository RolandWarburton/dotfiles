# optionally pick between a GUI and TERMINAL version of GNUPG pin entry
# pinentry-program /usr/bin/pinentry-curses
# pinentry-program /usr/bin/pinentry-qt
pinentry-program /home/roland/.local/scripts/pinentry-rofi
default-cache-ttl 3600
max-cache-ttl 3600
