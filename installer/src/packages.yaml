core:
  sudo:
  # git:
  # vim:
  # neovim:
  # tmux:
  # curl:
  # wget:

tools:
  htop:
  # ffmpeg:
  # fzf:
  # dnsutils:
  # gawk:
  # net-tools:
# servers:
#   ssh:
#   nginx:
#     postInstall:
#       - sudo $(which systemctl) disable nginx
#       - sudo $(which systemctl) stop nginx

# python:
#   python3:
#   python3-pip:

# shell:
#   zsh:
#     postInstall:
#       - sudo chsh -s $(which zsh) $(whoami)
