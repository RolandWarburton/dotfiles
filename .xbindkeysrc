"xselSpeaker.sh
    control + shift + s

"pkill aplay"
    control + shift + alt + s

#"( flameshot &; ) && ( sleep 0.5s && flameshot gui )"
#    Print

"screenshot.sh"
    Print

"flameshot gui --delay 3000"
    shift + Print

"sleep 0.2 && xdotool click 2"
    m:0x0 + b:10   (mouse)

"rofi -modi drun -show drun  -show-icons"
    Alt + space

"/home/roland/bin/lock.sh"
    control + alt + l

"xfce4-terminal"
    control + alt + t

"rofi -show window"
    control + Tab

"sudo pkill xbindkeys && xbindkeys"
    control + alt + shift + r

"jumpapp firefox"
    control + shift + F1

"jumpapp code"
    control + shift + F2

"jumpapp discord"
    control + shift + F3

"jumpapp alacritty"
    control + shift + F4

"redshift -0 4000k"
    Mod4 + b

"redshift -x"
    Shift+Mod4 + b

"printf warburtonroland@gmail.com | xclip -selection clipboard"
    Alt + Shift + e

"/home/roland/bin/swapMonitor -d left"
    Mod4 + Left

"/home/roland/bin/swapMonitor -d right"
    Mod4 + Right
